#include<stdio.h>
int main(void){
double height,weight,bmi;
printf("身長を入力:");
scanf("%lf",&height);
printf("体重を入力:");
scanf("%lf",&weight);
bmi=weight/(height*height)*10000;//bmiは体重÷（身長*身長）で求められます。
printf("%.1f\n",bmi);
return 0;
}
